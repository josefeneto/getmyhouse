# =============================================================================
# GetMyHouse - Environment Configuration
# =============================================================================

# Google Cloud Configuration
# ---------------------------
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# Application Settings
# --------------------
APP_ENV=development  # Options: development, production
LOG_LEVEL=INFO       # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# Database Configuration
# ----------------------
DB_PATH=./data/sessions.db

# Streamlit Configuration
# ------------------------
# Port for Railway deployment
PORT=8501

# Enable/disable features
# v2.0.3: Direct Web Scraping (NO LLM for search!)
USE_DISCOVERY_AGENT=false        # Not used in v2.0.3
ENABLE_REAL_SCRAPING=true        # ✅ RECOMMENDED: Uses direct web scraping

# How it works NOW (v2.0.3):
# 1. Direct scraping of real estate sites (Idealista, Imovirtual, etc)
# 2. Site-specific HTML parsers extract structured data
# 3. Fast (<1 second), free (no LLM tokens), reliable
# 4. Parallel execution for multiple sites
# 5. If no results found, falls back to mock data

# Benefits:
# ✅ Finds REAL properties online
# ✅ FAST - sub-second response time
# ✅ FREE - no LLM costs for search
# ✅ RELIABLE - site-specific parsers
# ✅ Robust fallback if scraping fails

# For Capstone Demo:
# Set ENABLE_REAL_SCRAPING=true to scrape REAL properties!
# Sites supported: Idealista.pt, Imovirtual.com (Portugal)

# Real Estate API Keys (Optional - for future use)
# -------------------------------------------------
# IDEALISTA_API_KEY=your_idealista_key_here
# IMOVIRTUAL_API_KEY=your_imovirtual_key_here

# Rate Limiting
# -------------
SEARCH_RATE_LIMIT=10  # Max searches per minute
CACHE_TTL=3600        # Cache time-to-live in seconds

# Observability
# -------------
ENABLE_TRACING=true
ENABLE_METRICS=true
LOG_FORMAT=json  # Options: json, text

# Development Tools
# -----------------
DEBUG_MODE=false
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_ENABLE_CORS=false
