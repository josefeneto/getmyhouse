# =============================================================================
# GetMyHouse - Environment Configuration
# =============================================================================

# Google Cloud Configuration
# ---------------------------
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# Application Settings
# --------------------
APP_ENV=development  # Options: development, production
LOG_LEVEL=INFO       # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# Database Configuration
# ----------------------
DB_PATH=./data/sessions.db

# Streamlit Configuration
# ------------------------
# Port for Railway deployment
PORT=8501

# Enable/disable features
# v2.1.5: Discovery Agent + LLM + Google Search
ENABLE_REAL_SCRAPING=true        # ✅ CRITICAL: MUST BE true FOR REAL SEARCH!

# How it works NOW (v2.1.5):
# 1. Discovery Agent finds real estate sites for ANY country (195 supported!)
# 2. LLM searches Google with site: filters for discovered sites
# 3. LLM extracts property data from Google Search snippets
# 4. NO hardcoded scrapers needed - works universally!
# 5. If search fails, falls back to mock data

# Benefits v2.1.5:
# ✅ Works for 195 countries (not just Portugal!)
# ✅ No hardcoded site scrapers needed
# ✅ Dynamic site discovery per country
# ✅ LLM + Google Search = Real properties
# ✅ Robust fallback if search fails

# ⚠️ IMPORTANT:
# - ENABLE_REAL_SCRAPING=true → Uses Discovery + LLM + Google (REAL search)
# - ENABLE_REAL_SCRAPING=false → Uses mock data only (for testing)
# 
# For production/demo: MUST be true!

# Real Estate API Keys (Optional - for future use)
# -------------------------------------------------
# IDEALISTA_API_KEY=your_idealista_key_here
# IMOVIRTUAL_API_KEY=your_imovirtual_key_here

# Rate Limiting
# -------------
SEARCH_RATE_LIMIT=10  # Max searches per minute
CACHE_TTL=3600        # Cache time-to-live in seconds

# Observability
# -------------
ENABLE_TRACING=true
ENABLE_METRICS=true
LOG_FORMAT=json  # Options: json, text

# Development Tools
# -----------------
DEBUG_MODE=false
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_ENABLE_CORS=false
